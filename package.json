{
  "name": "class-maker-ci",
  "private": true,
  "description": "Outillage de test/CI uniquement — l'application livrée reste 100% frontend, sans build (voir §7.1, README.txt). Rien ici n'est requis pour exécuter index.html.",
  "scripts": {
    "test:syntax": "find js -name \"*.js\" -print0 | xargs -0 -n1 node --check",
    "test:unit": "node test/c7_affinites.test.js",
    "test:smoke": "node test/smoke.test.js",
    "test": "npm run test:syntax && npm run test:unit && npm run test:smoke"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
